{
  "name": "DeltaOption",
  "scripts": {
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run --network localhost scripts/deploy.ts && node ../scripts/updateContractABI.js",
    "deploy:cronos": "npx hardhat run --network cronos scripts/deploy.ts && node ../scripts/updateContractABI.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "chai": "^4.3.6",
    "hardhat": "^2.10.1"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.4.2",
    "@openzeppelin/contracts": "^4.7.2"
  }
}
